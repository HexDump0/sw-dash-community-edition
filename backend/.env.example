# Stardance reviewer session cookie. Required on first run; after that
# the backend persists the rotated cookie in `backend/data/dash.db`.
STARDANCE_SESSION_COOKIE=

# Optional. With a token GitHub allows 5000 req/hr; without it 60 req/hr
# (the backend caches successful responses for 5 min regardless).
GITHUB_TOKEN=
